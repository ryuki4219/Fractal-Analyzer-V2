# フラクタル画像解析アプリ - トラブルシューティングガイド

## アプリが開けない場合の対処法

### 方法1: バッチファイルで起動（推奨）

1. `起動.bat` をダブルクリックしてください
2. 自動的にブラウザが開き、アプリが表示されます
3. もしブラウザが開かない場合は、手動で http://localhost:8501 にアクセスしてください

### 方法2: コマンドラインから起動

1. PowerShellまたはコマンドプロンプトを開く
2. このフォルダに移動する
3. 以下のコマンドを実行:
   ```
   streamlit run fractal_app.py
   ```
4. ブラウザで http://localhost:8501 を開く

### よくある問題と解決策

#### 問題: "streamlit: コマンドが見つかりません"

**解決策:** 必要なパッケージをインストールしてください
```powershell
pip install -r requirements.txt
```

#### 問題: ポート8501が既に使用されている

**解決策:** 別のポートで起動してください
```powershell
streamlit run fractal_app.py --server.port 8502
```
その後、http://localhost:8502 にアクセスしてください

#### 問題: ブラウザが自動的に開かない

**解決策:** 手動でブラウザを開き、http://localhost:8501 にアクセスしてください

または、自動でブラウザを開く場合:
```powershell
streamlit run fractal_app.py --server.headless=false
```

#### 問題: "ModuleNotFoundError" などのエラーが表示される

**解決策:** 
1. Python環境を確認:
   ```powershell
   python --version
   ```
   Python 3.8以上が必要です

2. 必要なパッケージを再インストール:
   ```powershell
   pip install --upgrade -r requirements.txt
   ```

#### 問題: アプリは起動するが、ページが真っ白

**解決策:**
1. ブラウザのキャッシュをクリア（Ctrl + Shift + Delete）
2. 別のブラウザで試す（Chrome, Firefox, Edgeなど）
3. ブラウザを再起動

#### 問題: アプリが非常に遅い、または応答しない

**解決策:**
1. アプリ内の「🧹 キャッシュをクリア」ボタンをクリック
2. Streamlitを再起動:
   - ターミナルで Ctrl+C を押して停止
   - 再度起動

### 動作確認

テスト用の簡易版アプリで動作確認できます:
```powershell
streamlit run test_streamlit.py
```

このアプリが正常に表示される場合、問題は `fractal_app.py` の特定の機能にあります。

### サポート情報

- Python バージョン: 3.8以上推奨（Python 3.13.5で動作確認済み）
- 必要なパッケージ: requirements.txt を参照
- ブラウザ: Chrome, Firefox, Edge推奨

### ログの確認

エラーの詳細を確認するには:
```powershell
streamlit run fractal_app.py 2>&1 | Tee-Object -FilePath log.txt
```

これにより、エラーメッセージが `log.txt` に保存されます。

### それでも解決しない場合

1. `test_streamlit.py` を実行して、基本的なStreamlit環境が動作するか確認
2. エラーメッセージの内容を確認
3. Python環境を再構築する
4. 仮想環境を使用する（推奨）:
   ```powershell
   python -m venv venv
   .\venv\Scripts\Activate.ps1
   pip install -r requirements.txt
   streamlit run fractal_app.py
   ```
